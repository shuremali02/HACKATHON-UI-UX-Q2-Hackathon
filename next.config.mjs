/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "i.pinimg.com",
            "s3-alpha-sig.figma.com",
        ]
    }
};

export default nextConfig;
